streamlit==1.39.0
pandas==2.2.3
numpy==2.2.1
matplotlib==3.9.2

# Excel
openpyxl==3.1.5
xlsxwriter==3.2.0
pyxlsb==1.0.10

# Parquet via fastparquet (no cmake toolchain needed)
fastparquet==2024.5.0
# cramjam is optional; remove it to avoid Rust build on Python 3.13

# Pin rich explicitly to avoid the platform upgrading it to 14.x
rich==13.9.4

